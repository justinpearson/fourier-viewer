Help me make an app to visualize 2D discrete fourier transforms. I'm not sure what's the best framework to use -- HTML, react, Python, PyGame, Godot, I'm not sure. I want it to be an interactive app where you see a grayscale image and its 2D fourier transform's amplitude and phase as 2 other grayscale images. There's a "pencil" tool that lets you "draw" on any image, thereby editing its pixel values, and the other 2 images are automatically updated. So for example, drawing a bunch of parallel lines on the space-domain image results in a bright spot at the appropriate spatial frequency and phase images. Or you can draw on the frequency plot, and the space-domain image is dynamically and continuously updated to display those added or removed frequencies. We should use discrete fourier transforms, not continuous ones. From a mathematical perspective, I am not sure how to constrain the editing process so that edits to the frequency amplitude or phase are done in such a way that the space-domain image remains real and not complex. Like, I forget the details, but the frequency amplitude and phase will have some symmetry due to the space-domain image being real-valued, and it would be good if our editing tool worked in such a way as to keep the space-domain image real-valued. This might mean that if you apply the pencil at position x in the frequency amplitude plot, it also edits -x. Or, we could pursue a different approach, and simply accept that the space-domain image may become complex-valued as the user edits the frequency amplitude / phase plots, and display its phase amplitude as a 4th image.